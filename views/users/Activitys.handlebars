{{>_homeNavbar}}
<div class="container">
    <div class="row">
        <div class="col-12 col-sm-12 col-md-12 col-lg-12 m-auto">

            <div class="mt-3">
                <center>
                    <h2>
                        <p> Atividades disponíveis</p>
                    </h2>
                </center>

{{#each evento.atividades}}
    <form action="/inscrever/atividade/{{dataValues.id}}" method="POST">
    <div class="card mt-2">
        <div class="card-body fundo">
                        <h3>{{dataValues.nome}}</h3>
                        <h6>
                            local de realização: {{dataValues.local_evento}} <br>
                            data de inicio: {{dataValues.data_inicio}}<br>
                            data de termino: {{dataValues.data_termino}}<br>
                            <!--input type="hidden" name="id_evento" value="{{id}}"-->
                        </h6>
                        <button type="submit" class= "btn btn-outline-secondary">inscrever-se</button>
        </div>          
    </div>
    </form>
{{/each}}

    </div>
        <div class="row">
            <div class="col-12 col-sm-12 col-md-12 col-lg-12 m-auto footer-pag-sem-cont">
                {{>_footer1}}
            </div>
        </div>
    </div>