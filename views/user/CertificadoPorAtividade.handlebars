{{>_navbar}}
<div class="mt-5">
    {{>_msg}}
    
    
    {{#each atividades}}
        <div class="card mt-2">
            <div class="card-body">
                <h3> Nome da Atividade: {{nome}} </h3><a href='/anhangueraeventos/formAtividades'><button class="btn btn-outline-secondary btn-tamanhoUnico">Gerar Certificado</button></a> 
               <input type="hidden" name="id" value="{{atividades.id}}">

             </div>
        </div>
    {{/each}}
</div>